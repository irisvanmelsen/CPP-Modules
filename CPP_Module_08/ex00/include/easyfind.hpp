#ifndef EASYFIND_HPP
#define EASYFIND_HPP

#include "string.h"
#include <iostream>
#include <algorithm>
#include <vector>
#include <vector>
#include <list>

// container of integers refers to a data structure that can hold a collection
// of integers (vector, map, unordered map etc)

template <typename T>
int easyfind(T &container, int target);

#include "easyfind.tpp"

#endif